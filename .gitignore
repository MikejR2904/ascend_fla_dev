# 机器与环境（绝不提交）
machine_specs.md
boards.json
*.local.json

# 构建产物与缓存
tmp/
# 不带斜杠：tools/dev_env.sh 支持 ASCEND_FLA_VENV，.venv 可能是指向别处的符号链接
.venv
# 多 agent 协作：每个任务的 worktree（docs/pm/START.md）
.claude/worktrees/
build/
dist/
*.egg-info/
__pycache__/
*.py[cod]
*.so
*.o
*.run

# ascriptor 生成产物
custom_op/
*-source/
kernel_meta/

# 真机与 profiling
*.csv
*.json.gz
prof_*/
msprof*/
ascend_*.log

# 权重与数据
*.safetensors
*.bin
*.pt
datasets/

# macOS 垃圾文件
.DS_Store
._*
