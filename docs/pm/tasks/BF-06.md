# BF-06 KDA decode（fused_recurrent）：BF16 原生 kernel，A5 真机

- 波次 / SoC：W0 / a5 · 优先级：P0（用户 D-PM-35：后续 BF16 优先）· 时限：16h · 依赖：`A2-44`（已完成）
- 需要：ascriptor、fla、a5 真机（D-PM-34）
- 写集：`kernels/projects/a5/kda_fused_recurrent_bf16/**`、`ascend_fla/ops/kda/fused_recurrent.py`（只加 BF16 分派，FP32 路径与 A2-44 的 raw flags 语义逐位不变）、`tests/test_kda_decode_bf16.py`、`docs/research/kda_decode_bf16.md`
- **先读 `docs/pm/bf16-kernel-side.md`**（什么算 kernel 侧、统一验收、陷阱）；本文只写这条任务特有的部分。

## 这条任务是怎么来的

用户 2026-09-19（D-PM-35）：BF16 计算必须在 kernel 里做，不能在 host 侧完成；后续 BF16 优先。KDA decode 入口收任意浮点 dtype，但在 host 侧 `.to(torch.float32)` 后进 FP32 kernel（`ascend_fla/ops/kda/fused_recurrent.py`），BF16 计算不在 kernel 里。

## 范围

- 域与现有 decode 一致：`T≤16`、`q,k:[B,T,H,128]`、`v:[B,T,HV,128]`、`g:[B,T,HV,128]`、`beta:[B,T,HV]`、`initial_state:[B,HV,128,128]` FP32（K 在前）；
  BF16 的 `q/k/v` → BF16 的 `o`，`final_state` FP32。kernel 里读 BF16、在 kernel 内转 FP32 做递推（decode 量很小，精度自选，写进文档）。
- 起点（只读）：现有 `kernels/projects/a5/kda_fused_recurrent`（FP32 单元）。新建 `kernels/projects/a5/kda_fused_recurrent_bf16/`。
- A2-44 的 raw flags（`use_*_in_kernel`）预处理仍走现有路径（host 侧 FP32 预处理后再 cast 成 BF16——融合进 kernel 是 `BF-07` 的事），本任务不改它，也不新增 host 转换。
- **prefill→decode 一致性是硬判据**：BF16 chunk 前向（原生 KDA，`kda_fwd_stable`）的 `final_state` 作 BF16 decode 的 `initial_state`，逐 token 递推与一次性长前向对得上。

## 验收

- [ ] 通则 §1：BF16 公共入口的 host 算子审计（真机，附算子列表）；入口源码里不再有 BF16 路径的 `.float()` / `.to(dtype)`。
- [ ] 通则 §2：BF16 校准先行（误差地板 F 与预算写进 `docs/research/kda_decode_bf16.md`，实现前定死）。
- [ ] 通则 §3 真机：完整 workload 先跑；`T ∈ {1,2,16}` × `HV/H ∈ {1,2,4,8}` × 有 / 无 `initial_state`，`bd ∈ {1,2,4}`（现有 decode 支持的取值）；`bd` 间输出逐位相同；输入不被修改；NaN 预填。FP32 路径改前改后逐位相同。
- [ ] 通则 §5：同卡三轮三明治，BF16 新路径 vs 现有 FP32 路径，如实标注 baseline，不设速度门槛。
- [ ] 入口拒绝范围外的组合并说清哪条约束不满足；`git diff --stat` 对 `reserved_paths` 为空；没有改 FP32 单元源码。
- [ ] 预算：`o` 对 A（CPU FP32 递推，喂 BF16 舍入后的输入）相对 L2 ≤ 1e-2 且 ≤ 3F；`final_state`（FP32，输入同为 BF16 值、状态不经 BF16 舍入）≤ 1e-5。
- [ ] 每次调用的 host 固定成本不增（`AGENTS.md` §6 铁律三：缓存键 O(1)）；报一次单步调用的固定成本，不设门槛。
