# PK-04 PKDA chunk 前向：A5 真机验收补齐（PK-02 的后续）

- 波次 / SoC：W0 / a5。**真机验收**：PK-02 只资格化了主机侧（`validation.json` 的 `untested` 里有 vendor 编译、board、aclnn、
  inprocess NPU）；本任务在 A5 真机上补齐，**不借用 PK-03 或历史 KDA 的验收**。合入需要用户明确授权
- 优先级：`-` · 时限：16h（真机部分若因机器/卡自身受阻，assignee 在 STATUS 里如实说明，PM 据此调整）· 依赖：`PK-02`（已完成）
- 需要：A5 真机、ascriptor workspace（按 `agent/compatibility.json` 的 pin）、fla（pin `e52dbc0ea19d3a40d7ab7f9eed855d2b473994d2`）
- 写集：**与 PK-02 的四条写集完全相同，不扩大**：`kernels/projects/a5/pkda_chunk_fwd/**`、`ascend_fla/ops/pkda_chunk_fwd.py`、
  `tests/test_pkda_chunk_fwd.py`、`docs/research/pkda_chunk_fwd_gate_range.md`

## 这条任务是怎么来的

PK-02（PR #88）合入时只做了主机侧。#68 下的一条 REQUEST（PK-02 的申领 session）请求补 A5 真机验收，并自述用户最新明确要求
"需要完成真机测试"。PM 分诊后采纳；来源是外部需求，所以在用户批准前是 gated。

## 范围：不改 PK-02 已批的公开 ABI 与预算

FP32 only、q/k 原样消费（调用方负责归一化）、预测用原始 `k`/写入用 `k_precond`、两个状态独立、B1/2、T1..4096、H1..32、
K=V=128、前向跨度门控 155、`block_dim ∈ {1,2,3,4}`。**失败时**：先定位所属层（源码/编译/运行时/数值），再在已批 PKDA 写集内
修复并回归；修复若需要写集外的文件，先发 `RISK write-set-expansion`，批之前别动。

## 验收

- [ ] **全部五个 vendor 在首次自定义 kernel 执行前编完**；每个 `block_dim` 独立进程、一个算子名一份 build（`AGENTS.md` §5、§6）。
- [ ] **完整 `B1/T4096/H8/K=V128` workload 先跑**：实际输出，以及全部 **14 个阶段**，对运行时生成的独立 CPU FP32 chunk（B）与
      pinned FLA naive（A）；`o` / `final_state` / `final_A_state` 相对 L2 各 ≤ 1e-4，逐项报数字。
- [ ] **16 个契约 case**、尾块（T 非 64 的倍数）、独立双状态与续接（非零初始 state 与 A）、前向门控 155（含域内对抗性跨度）、
      `block_dim ∈ {1,2,3,4}`；**保留逐阶段哈希，`bd=1..4` 输出逐位相同**（`AGENTS.md` §6 硬判据）；输入不被修改。
- [ ] 同卡同步三轮 baseline/candidate/baseline 性能三明治，**基线如实标注身份**，**不设速度门槛**，原始样本保留。
- [ ] 保持 FP32 only、raw q/k、读/写 key 不对称；公开 ABI 与预算不变；**没有声称** BF16、backward、Triton、checkpoint。
- [ ] 更新范围与真实证据（每个真机数字带 SoC、CANN、算子包目录与原始日志行）、可恢复的脱敏归档；以新 PR 交付；
      **全部真机项完成后才 DONE**。
- [ ] `git diff --stat` 对 `reserved_paths` 为空，且没有改动写集之外的文件。

## 已知陷阱

- PK-02 里"A 相对 L2 为 0"**只对 CPU 模型成立**（参考与 kernel 的 ATK 都是与 naive 同顺序的 FP32 逐元素运算）；真机的 `exp/ln`
  不保证逐位相同，预算仍是 ≤ 1e-4，别拿"逐位相同"当真机的期望。
- 跨度 4096 的 FP32 前缀差抵消误差 S=1.34e-4 超预算，已保留在公开域之外（门控 155）；不要因为真机上某个 case 通过就放宽门控。
- 机器与卡归 assignee 自己管理（D-PM-28）；PM 只看进度与代码/证据质量。
