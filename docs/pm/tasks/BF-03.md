# BF-03 PGDN chunk 前向：BF16 原生 kernel（含 ATK 与 q/k 归一化），A5 真机

- 波次 / SoC：W0 / a5 · 优先级：P0（用户 D-PM-35：后续 BF16 优先）· 时限：24h · 依赖：`PK-03`（已完成）、`BF-01`（**完成后才可派**）
- 需要：ascriptor、fla、a5 真机（D-PM-34）
- 写集：`kernels/projects/a5/pgdn_chunk_fwd_bf16/**`、`ascend_fla/ops/pgdn_chunk_fwd.py`（只加 BF16 分派，FP32 路径逐位不变）、`tests/test_pgdn_chunk_fwd_bf16.py`、`docs/research/pgdn_chunk_fwd_bf16.md`
- **先读 `docs/pm/bf16-kernel-side.md`**（什么算 kernel 侧、统一验收、陷阱）；本文只写这条任务特有的部分。

## 2026-09-19 追加（用户 D-PM-37）：格式转换也在 kernel 内，FP32 路径同样

- 分组（q/k 头共享）只能靠 kernel 内 head 索引，不得在 host 复制 / 重排 q/k；ATK 的 per-`H` 状态同理。FP32 路径（PK-03 现状）同样去掉 host 转换，输出改前改后逐位相同，两条路径都过 host 算子审计。

## 这条任务是怎么来的

用户 2026-09-19（D-PM-35）：BF16 计算必须在 kernel 里做，不能在 host 侧完成；后续 BF16 优先。PGDN 前向（PK-03）现在的 BF16 入口是 `q.float()` → FP32 kernel → 输出转回（`ascend_fla/ops/pgdn_chunk_fwd.py`），host 侧加宽。

## 范围

- 域与 PK-03 逐项一致（GDN 分组域 + ATK：`g_atk` / `beta_atk` / `log_atk_scale` per-`H`，`initial_state` / `initial_A_state` 只接 None，归一化必须开启，`block_dim∈{1,2}`）。
  BF16 的 `q/k/v` → BF16 的 `o`；`final_state`、`final_A_state` 仍 FP32。
- **q/k 的 `F.normalize`（`eps=1e-12` 语义，PK-03 已接受）必须在 kernel 里做**：BF16 q/k 进 kernel，kernel 内 FP32 归一化后再作 cube 的 BF16 operand；host 不得预归一化。
  ATK 递推、有界 squash、读 / 写 key 不对称（`k_read` / `k_write`）保持 PK-03 的语义，在 kernel 里。
- 起点（只读）：PK-03 的 FP32 单元与 `BF-01` 的 BF16 GDN 单元。新建 `kernels/projects/a5/pgdn_chunk_fwd_bf16/`。

## 验收

- [ ] 通则 §1：BF16 公共入口的 host 算子审计（真机，附算子列表）；入口源码里不再有 BF16 路径的 `.float()` / `.to(dtype)`。
- [ ] 通则 §2：BF16 校准先行（误差地板 F 与预算写进 `docs/research/pgdn_chunk_fwd_bf16.md`，实现前定死）。
- [ ] 通则 §3 真机：完整 workload 先跑；分组比 `{1,2,4,8}` × `C ∈ {1,2,3,64}`、多 batch、零 / 近零 q/k 行、`beta_atk=0`；`bd` 间输出逐位相同；输入不被修改；NaN 预填。FP32 路径改前改后逐位相同。
- [ ] 通则 §5：同卡三轮三明治，BF16 新路径 vs 现有 FP32 路径，如实标注 baseline，不设速度门槛。
- [ ] 入口拒绝范围外的组合并说清哪条约束不满足；`git diff --stat` 对 `reserved_paths` 为空；没有改 FP32 单元源码。
- [ ] 预算：`o`、`final_state` 对 A（pin 版 PGDN naive，喂 BF16 舍入后的输入）与 B（独立 CPU 参考）各相对 L2 ≤ 1e-2 且 ≤ 3F；`final_A_state`（FP32 对角状态）≤ 1e-4。

## 已知陷阱

- PGDN 的 naive **做** `F.normalize`，PKDA 的不做（`docs/research/pkda_semantics.md` §7）；别把两个算子的结论互相套用。
- 近零行的归一化在 BF16 下分叉最容易被漏掉：网格必须含近零行，并量化写明与 FP32 路径的差。
