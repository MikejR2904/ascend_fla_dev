# PK-02 PKDA chunk 前向：ATK 预条件 + KDA 派生单元（主机侧）

- 波次 / SoC：W0 / a5。纯主机侧：ascriptor check、reference、缩小形状 sim/pipesim。真机验证是后续任务
- 优先级：P2 · 时限：24h · 依赖：PK-01（已完成，见 `docs/research/pkda_semantics.md`）
- 需要：ascriptor workspace（`AGENTS.md` §3 的 gitcode 来源）、fla（本地 checkout 或已装的 0.6.0+，
  需要包含 `fla.ops.precond_kda`——2026-06 才合入上游，比本仓 `.venv` 里装的 0.5.2 新）
- 写集：`kernels/projects/a5/pkda_chunk_fwd/**`、`ascend_fla/ops/pkda_chunk_fwd.py`、
  `tests/test_pkda_chunk_fwd.py`、`docs/research/pkda_chunk_fwd_gate_range.md`

## 2026-09-19 口径补充（随派单，回答申领人的提问；与 PK-03 对齐）

1. **dtype 与比较边界**（`AGENTS.md` §6：算子正确性一律在 FP32 下判定）：`o` / `final_state` / `final_A_state`
   三者对 fla naive（CPU FP32）相对 L2 ≤ 1e-4，对独立 CPU 参考也各报一组。派生单元可以是**全 FP32 的五阶段实现**
   （参照 GD2-01 的 5-launch 结构），这满足预算，**不放宽**。BF16 公共输入/输出**不是验收项**：若提供，只作输出质量
   检查（≤ 5e-3，报数，不作对错判定），并写明 BF16 量化发生的位置与 FLA 归一化 cast 的边界；**不得**把现有 BF16 KDA
   的证据当作 PKDA 的证据。看板 dtype 列的 `bf16,fp32` 读作"FP32 判定 + BF16 可选质量检查"。
2. **oracle 口径**：A = fla `precond_kda` 的 `naive.py`（CPU FP32，语义权威）；B = 不复用 A 代码的独立 CPU 参考
   （chunk ↔ recurrent 互为 oracle）。fla Triton chunk 实现**不要求，也不得声称验证过**（无 GPU，且与 naive 在近零行
   归一化上不等价）。下面步骤 4 与"目标规模"里"以 Triton chunk 作第二 oracle"的旧说法以本条为准。
3. **归一化语义**：kernel 取 naive 语义（`AGENTS.md` §1）；naive 与 chunk 路径在近零行的归一化不等价，
   `pkda_chunk_fwd_gate_range.md` 要写明并量化（同 PK-03：‖x‖ 取 1e-2、1e-4、1e-6、0 各一档）。
4. **k 的读/写不对称**（申领人读 pin 住的源码后的自述，PM 未独立核实，冻结 ABI 时自己对源码证实或更正）：
   `naive.py` 的残差预测用原始（已归一化的）`k`，只有写入用 `k_precond`；把 `k` 整体换成 `k_precond` 会改变递推。
   派生的 scores/WY 必须保持这处不对称。
5. **真机**：本任务的验收是主机侧（同上文）。assignee 有真机可以加真机数字作为**额外**证据，机器与卡归 assignee
   自己管理（D-PM-28），PM 不设关卡；额外的真机数字要带 SoC、CANN、内置算子包目录和原始日志行，且不能替代主机侧验收。
   如果用户要求 PK-02 也必须真机验收（像 PK-03 的 D-PM-26），PM 会另行更新本规格。

## 这条任务是怎么来的

`docs/research/pkda_semantics.md`（PK-01）确立了权威来源：论文 *Preconditioned DeltaNet*
（[arXiv:2604.21100](https://arxiv.org/abs/2604.21100)，ICML 2026），已合入上游 fla
（PR [#950](https://github.com/fla-org/flash-linear-attention/pull/950)）。**PKDA 是
KDA 加一层 ATK 预条件，不是新算子族**——ABI 除了三个 ATK 专属张量，其余与本仓
`ascend_fla/ops/kda/chunk.py` 逐项对应。本任务只做前向。

**先读 `docs/research/pkda_semantics.md` 全文再动手**，尤其是 §4（ATK 算法与量程结论）
和 §7（已知陷阱）。这里不重复，只列本任务specific的步骤。

## 目标规模（没有已发布权重，见 pkda_semantics.md §5）

论文训练仓的真实配置：`hidden=1024/1792, heads=8/14, head_dim=128/120, layers=24`
（340M/1B 两档）。**没有可下载的预训练 checkpoint**——本任务到不了"真实 logits"这一级，
双 oracle 用 fla 的 naive.py（CPU fp32）与独立 CPU 参考（口径见上面"口径补充"；fla 自己的 Triton chunk 实现
已有 78 个上游测试验证过，但本任务不要求、也不得声称验证过它），不需要真实权重去补强。

## 步骤

1. **确认 ABI，逐项对照 KDA**：把 `chunk_precond_kda` 的签名（`fla/ops/precond_kda/chunk.py`）
   与 `ascend_fla/ops/kda/chunk.py` 的现有参数逐项列表对比，写进
   `docs/research/pkda_chunk_fwd_gate_range.md` 开头。确认 `use_gate_in_kernel`/`safe_gate`/
   `lower_bound` 在本仓 KDA 实现里的具体语义，不要凭参数名猜。
2. **ATK 预条件的精度复核**（`pkda_semantics.md` §4 已给出算法，`M` 恒有界不需要重新证明，
   但下面两处要用本仓真实会遇到的量级重新测）：
   - `log(A_t + eps)` 在 `k` 经过 `use_qk_l2norm_in_kernel` 归一化之后的真实量级下，
     FP32 精度是否够（`A_t` 是 `beta_atk · k²` 的累加，`k` 归一化后逐元素量级在 `O(1/√K)`）。
   - `k_precond = k · M` 替代原始 `k` 进入 causal score / WY 三角解之后，KDA 自己那套
     门控跨度判定（前向 155、反向 105）要不要收紧——把 `AGENTS.md` §6 的"整条链同类算式
     列一遍"方法论对 PKDA 的完整前向链重新过一遍，不是只看 ATK 那几步。
3. **建单元**：`kernels/projects/a5/pkda_chunk_fwd/`，按 ascriptor unit 协议
   （`unit.py`/`contract.json`/`run.py`）。**复用本仓 `kda_fwd_stable` 的 causal score / WY
   三角解 / 状态扫描 / 输出组合分解**（参照 GD2-01 的 5-launch 结构做法），只在"算 `k` 参与
   三角解"之前插入 ATK 预条件这一步（多一次 launch 算 `A_t`/`M`/`k_precond`，或者融合进
   现有的 prepare 阶段——两种都可以，权衡好写 `docs/research/pkda_chunk_fwd_gate_range.md`）。
   `initial_A_state`/`final_A_state`（`[B,H,K]`）是独立于主状态 `[B,H,K,V]` 的第二个递推量，
   contract 里两个状态分开声明。
4. **双 oracle 交叉验证**：
   - 对 `fla.ops.precond_kda.naive.naive_recurrent_precond_kda`（CPU fp32）：`o`/`final_state`/
     `final_A_state` 三者都要给相对 L2，不能漏掉 `final_A_state`。
   - 对不复用 naive 代码的独立 CPU 参考（口径补充第 2 条）：同样三者各报一组。**不**要求 Triton chunk；
     引用 fla 上游测试的结论不能替代本仓单元自己的验证。
   - chunk 与逐 token recurrent 数学等价（AGENTS.md §6 硬判据）：至少一个 toy 形状 +
     一个真实规模形状（用 §（目标规模）表里的 340M 配置）。
5. `run.py reference` 覆盖真实规模的 T 网格（借用 KDA 现有 case 的 T 边界：≤64、跨 chunk、
   长序列），`run.py check --launcher sim`/`pipesim` 只在缩小形状的边界组合点上跑
   （不要重复 GD2-01 那次犯过的"sim 里塞全形状网格"的错，直接照 GD2-01 更正后的做法来）。
6. 门控：`ascend_fla.ops.pkda_chunk_fwd` 的入口，两个状态的独立性、shape 不匹配、
   不满足定尺时显式报错（AGENTS.md §7）。

## 验收

- [ ] `docs/research/pkda_chunk_fwd_gate_range.md`：ABI 对照表、ATK 精度复核结论
      （`log(A+eps)` 与 `k_precond` 量级判定）、KDA 门控跨度是否需要收紧的结论。
- [ ] `ascriptor check` 在新单元上 0 error。
- [ ] `run.py reference` 在契约 case 全过；`run.py check --launcher sim/pipesim` 在缩小
      形状诊断点上过。契约的 `board`/`aclnn` stage 标 `untested`。
- [ ] 双 oracle 数字齐全：fla naive 的 `o`/`final_state`/`final_A_state` 三者相对 L2 ≤1e-4；
      chunk↔recurrent 等价性数字（toy + 真实规模形状各一个）。
- [ ] 复用关系写清楚：具体点名复用了 `kda_fwd_stable` 的哪几个 kernel/哪部分逻辑，
      新增的 ATK 步骤是独立 launch 还是融合进现有阶段，为什么。
- [ ] 门控入口对不满足定尺、状态维度不匹配报错，不静默。
- [ ] 没有声称任何真实 checkpoint 的 logits 验证——本任务到不了那一级，如实说明。

## 验证命令

```bash
PYTHONPATH=<ascriptor>/library python kernels/projects/a5/pkda_chunk_fwd/run.py reference
PYTHONPATH=<ascriptor>/library python kernels/projects/a5/pkda_chunk_fwd/run.py check --launcher sim
python -m pytest tests/test_pkda_chunk_fwd.py -q
python tools/gen_matrix.py --check
python tools/pm_board.py --check
```

## 已知陷阱

- `fla` 0.5.2（本仓 `.venv` 已装的版本）**没有** `precond_kda`——需要 0.6.0，或一份 pin 在 revision
  `e52dbc0ea19d3a40d7ab7f9eed855d2b473994d2` 的本地 checkout（与 GD2-01 用的是同一个 fla revision）。
- `A_state` 与主状态是两个独立的递推量，形状维度数不同（`[B,H,K]` vs `[B,H,K,V]`）——
  混淆会在形状检查报错，但也可能凑巧广播成功变成静默错误，入口要显式分开检查。
- `run.py` 的位置参数是 `{reference,check,profile}`，`sim`/`pipesim` 是 `check` 的
  `--launcher` 取值（GD2-01 踩过这个坑，见其 spec 的更正记录）。
- 不要假设 ATK 的 `M` 恒有界这条结论意味着整条链都安全——它只保证 `M` 本身不溢出，
  `k_precond` 进入后续三角解之后的精度仍要重新测（见步骤 2）。
