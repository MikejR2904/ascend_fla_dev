# A2-07 Qwen3-Next 上 A2 的 GDN ABI 方案（调研 + 设计，不写 kernel）

- 波次 / SoC：W0 / a2（纯主机侧，桌面调研 + `ascriptor check` / sim 取证）
- 优先级：P1 · 时限：12h · 依赖：A2-01
- 需要：ascriptor workspace、fla（对照 GDN 语义）
- 写集：`docs/research/a2_gdn_abi.md`

## 目标

首波的第二个模型是 Qwen3-Next-80B-A3B，走 GDN。它被六项 ABI 缺口挡着，而且全是 kernel 改动。本任务要产出一份
能直接拆成 kernel 批次（A2-K1 的 GDN 部分）、交给用户审批的设计文档。

## 背景（摘录）

Qwen3-Next 的形状见 `docs/matrix/models.json`：hidden 2048、key heads 16、value heads 32、head_k = head_v = 128、conv 4。

上游 `a5/gdn_fwd` 五阶段对着 fla 是这么映的：preprocess（g_cumsum / decay_mask）↔ `gated_delta_rule/gate.py` 加 cumsum；
inverse（严格下三角求逆）↔ `ops/utils/solve_tril.py`；recompute ↔ `wy_fast.py`；scores ↔ `chunk_o.py`；
recurrent ↔ `chunk_delta_h.py`。`gdn_bwd` 含全部反向保存值。

六项缺口在 `gaps.json` 里：`gdn-no-gqa`（domain 只有 B,H,C，没有独立的 value-head 维）、`layout-not-token-major`
（公开布局是 `[B,H,C,L,D]`）、`nonzero-initial-state`、`d-initial-state-absent`（反向不出 dh0）、`state-dtype-bf16`、
`fused-recurrent-missing`（GDN decode kernel 整族缺失）。另外还牵着 `scale-param-no-slot` 和 `no-tail-path`。

KDA 在这六项上都已经具备（`AGENTS.md` §2 的对照表），所以 KDA 的实现就是现成的参照：GQA 用 `HV % H == 0`、
token-major 公开布局在内部 permute、FP32 state 进出、反向出 dh0、decode kernel 一核一头不需要核间同步
（`a5/kda_fused_recurrent`）。

## 交付文档须含

1. 逐缺口写一遍：现状（引用上游 kernel 源码行）→ 改法设计（参照 KDA 对应实现）→ 影响的 kernel 列表 → 契约变更 →
   数值风险（量程、dtype）→ 验证计划（fp32 双 oracle：fla naive 与 chunk↔recurrent 互验）→ 工作量估计。
2. A2-01 命中表里 GDN 各 kernel 的 split-K 暴露，以及它对设计的影响。
3. 按 `AGENTS.md` §6「量程」那一节，把 GDN 链上所有 `exp` / 除法 / cumsum 类算式逐处列表判量程。真实初始化下的
   门控跨度要单独估，**不要照搬 KDA 的 94**。
4. Qwen3-Next 其余层（gated full attention、MoE、RMSNorm 等）在 910B 上需要哪些 torch_npu 算子，列一份清单。
   真机覆盖要等到 A2-10 才测得了，这里只列清单和"缺了会怎样"。
5. 建议的 kernel 批次怎么拆：每批的内容、依赖、预计验收，供 PM 交用户审批。

## 2026-09-21 更新（PM；规格写于 09-19，之后 main 变了，也补上真机验证的最低清单与写集）

- **A2-01 已经 done**（PR #107 已合入）：命中表在 `benchmarks/a2/evidence/host/hit_table.json`，真机回执在 `benchmarks/a2/evidence/`，结论在 `benchmarks/a2/README.md` 与 gaps 的 `a2-splitk-fp32-cube`、`a2-splitk-bf16-fp16-unsettled`（含「profile 声明的 20 / 40 与 `torch_npu` 报的物理核数一致」）。交付第 2 条必须**按 A2-01 的实际命中表**写：GDN 各 kernel × dtype × M 形状里，哪些是 FP32 手写 MMAD 累加链（KDA / GDN 的三角求逆与 GDN 反向 finalize 各一处，M16 形状）、哪些是 BF16 / FP16 split-K（Qwen3-Next 是 BF16，直接相关）。
- **对着 main 现状写**：09-19 之后 main 已有 GDN 的派生单元与公共入口——GDA-01（A5 前向）/ GDA-02（GQA/GVA 分组，kernel 内 head 索引）/ GDA-03（分组反向）/ BF-01、BF-02（BF16 原生前向 / 反向），路径 `kernels/projects/a5/gdn_chunk_{fwd,fwd_bf16,bwd,bwd_bf16}`、`ascend_fla/ops/gdn_chunk_{fwd,bwd}.py`。六项缺口要逐项核对**这些派生单元的代码**：哪些已经有做法（写文件:行）、移到 a2 还缺什么、哪些真没有（decode：GDA-04 仍 open）；批次建议相应写成「派生单元移到 a2 + 剩余缺口」，不是「从上游契约重做」。gaps.json 里六条缺口记的是**上游** `a5.gdn_fwd/bwd`（见 `gdn-no-gqa` 的 2026-09-18 注），别把它当成派生单元的现状。BF16（D-PM-35 / 37）：加宽 / 收窄必须在 kernel 里，设计里明确写。
- **真机验证最低清单**（D-PM-34；在你的 910B3 上测，写原始日志行、SoC、CANN 版本、内置算子包目录、逐项数字；A2 数字在 A2-11 之前只是观测记录）：① §4 清单里每个 torch_npu 算子的「能否跑」（bf16 与 fp32，原文报错）；② 量程表依赖的 FP32 `exp` 上 / 下溢线在该 SoC 自编译向量路径上的实测（含 subnormal 是否被冲零——A5 上是，a2 未知）；③ 核数引用 A2-01；`block_dim` 上限与超限行为**没人测过**，要写就测（一个进程一份，超物理核数可能在硬件 barrier 上死锁，要能收场），不测就别写；④ 设计里最关键的一个实现件（比如 GDN 衰减 / 中点分解的最小原型，或一头一核的 decode 骨架）在 a2 上 `ascriptor check` + 真机运行。
- **写集补（PM 事先批准）**：`benchmarks/a2/probe_gdn_abi.py`、`benchmarks/a2/evidence/gdn_abi/**`（真机探针与原始证据）；仍不改任何 kernel / `ascend_fla/**`。
- **引用规则**：文档里的每条事实要么来自仓里存在的文件（写路径 / 行）、要么来自你在真机上测到的（写日志），查不到出处的删掉；Gemini 规划文档只当需求来源（`docs/pm/PROTOCOL.md` §6）；跨 SoC 规则在 `AGENTS.md` §6「结论不跨 SoC 继承」；`GetVecNum() == 2 × block_dim`（§6），别把向量核数写成 block_dim 上限。

## 验收

- [ ] 六项缺口每项都有完整的 1 中各栏，无"类似 KDA"一笔带过。
- [ ] 量程表覆盖 GDN 链全部相关算式，逐处给出结论（包括"恒 ≤1、下溢到 0 是正确结果"这类判断）。
- [ ] 批次建议可直接落到看板。
- [ ] 没写或改任何 kernel。
- [ ] **真机验证（用户 2026-09-19，D-PM-34：所有任务必须完成真机验证）**：交付物要在真机上跑过并带证据——SoC、CANN 版本、内置算子包目录、原始日志行、逐项数字；结论只对你实际跑过的那个 SoC 成立（`soc: any` 的任务在你自己的真机上验证并写明是哪个，不外推）。主机侧/CPU 测试保留，但不能替代真机验证；DONE 里没有真机验证就 `REVIEW rework`。设计/调研类交付：把设计所依赖的、能在真机上实测的事实（算子包覆盖、dtype/指令支持、核数/UB/block_dim 上限、原型编译与运行）在真机上测出来报数字。涉及 a2 的另遵守 `AGENTS.md` §2：A2-11 之前，A2 真机数字只作观测记录，不构成算子结论。
