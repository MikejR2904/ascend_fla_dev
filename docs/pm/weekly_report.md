# ascend_fla 周报

维护方式：每次更新在本文件**顶部**插入新一节（最新一周在最上面），旧记录整节保留、不删除不覆盖。
数据来自 `docs/pm/board.json` 与 `docs/matrix/{ops,gaps}.json` 在写入当天的快照，PM 从原始任务记录逐项核对后写入，不是转述申领人自述。任务状态词汇：`done`（A5 上已过真机验收；A2 数字在 A2-11 之前只算观测，不计入 done）/ `in_progress` / `rework` / `open` / `gated`。

---

## 2026-09-22

### 一级：一句话总结

61 项任务完成 31 项（51%）：A5 的 KDA 全链路（前向/反向/decode）与 GDN 前向反向已过真机验收，A2 只有 KDA 三个单元与运行时桥有真机观测（未计入完成，等 A2-11）；当前最大的两个风险是 KDA 训练前处理反向让整训练步比旧路径慢 11 倍（BF-09 待认领）和 GDN-2 BF16 前向卡在共享机器抢卡上、时限已超但无数值 ETA（BF-05）。

### 二级：算子 → 架构 → dtype

计划内 6 个算子族 × 3 条通路（前向/反向/decode）= 18 个算子槽位；另有 4 个整网融合算子（causal_conv1d、fused_rms_norm_gated、qk_l2norm_gate、packed_projection）单列。

**A5（首要 SoC）**

| 算子族 | 前向 | 反向 | decode |
|---|---|---|---|
| KDA | ✅ 完成 | ✅ 完成（P0 遗留：A5K-03 dh0 不确定性未定位） | ✅ 完成 |
| GDN | ✅ 完成（含 GQA/GVA） | ✅ 完成（含 GQA/GVA） | ⬜ 未开工（骨架 GDA-04） |
| PGDN | ✅ 完成 | ⬜ 未开工（骨架 PK-05） | ⬜ 未开工（骨架 PK-06） |
| PKDA | ✅ 完成 | — 未排期 | — 未排期 |
| GDN-2 | 🟡 FP32 完成 / BF16 进行中（BF-05，受阻于抢卡，已恢复，仅拿到 1 个 case 的真机 pass） | — 未排期 | ✅ 完成（仓主轨道，PM 未复核） |
| DeltaNet | — 未排期 | — 未排期 | — 未排期 |

A5 小计：完成 8/18（44%），进行中 1/18，开工 9/18（50%）。

**dtype 细分（A5 已完成的 8 个算子槽位）**
- BF16 原生 kernel：8 个（KDA 前向/反向/decode、GDN 前向/反向、PGDN 前向、PKDA 前向、GDN-2 decode），全部满足 `min(1e-2, 3F)` 预算，最紧占预算比例集中在 0.33（校准即预算下限，非放宽）。
- FP32 独立路径：6 个（GDN 前向/反向、PGDN 前向、PKDA 前向、GDN-2 前向、KDA decode），预算 1e-4，典型最差相对 L2 在 1e-6～1e-5 量级。
- 已知精度披露（不算通过，用户已批准披露口径 D-PM-52）：KDA 前处理 chunk/decode 近零下溢位置各 40/148 处；KDA 训练前处理反向每 bd 482 个 native 下溢输出、30 个 gate 上限观察点。

**A2（第二 SoC，A2-11 前均为观测）**
- 开工 3/18：KDA 前向（20 案例）、反向（26 案例）、decode（8 案例）已在 910B3 上过 aclnn harness；运行时桥对 harness 逐位一致 152/152（A2-10，rework 中，等三处测试断言补齐）。
- A2-01 定性发现：FP32 split-K 已有修复路径逐位通过；BF16/FP16 split-K 在 M32 形状上会触发 AI Core 异常（P0 未解决）。
- GDN A2 ABI 方案已完成设计（A2-07），实现（A2-20）等你批准 kernel 批次。
- 总闸 A2-11（split-K 绕行验证）尚未开始，A2-12～A2-16、A2-K1 全部卡在它后面。

**A3**：0/18，仅占位任务 A3-00，未开工。

**整网融合算子（主机侧 torch 版，自编译 kernel 0/4 开工）**
- 清单与验收设计已完成（A2-40）。
- 4 个 A2 自编译单元任务（A2-41～A2-43）全部 gated，等 kernel 批次批准。

**质量与缺口**
- 主干无 NPU 全量测试：1173 passed / 12 skipped。
- 支持矩阵缺口表：54 条（13 条已解决），未解决 P0 2 / P1 23 / P2 16。两个 P0 均是静默错误类：`c1-multihead-o-corrupt`（多头写入内容错误，已定位未修）、`ascriptor-gm-transfer-two-slice-row-gap`（上游库缺陷，行间距算错）。

**当前在飞任务**
- **BF-05**（GDN-2 前向 BF16，P0）：24h 时限已超约 10h，两天来在共享机器上因健康卡与空闲卡不重叠而多次停摆；今天恢复后拿到第一个真机 case 通过（`t1_h1_small`），12 案例全量正在跑，尚未给出数值 ETA，PM 已再次索要。
- **BF-09**（KDA 前处理反向性能优化，P0）：已派，等 ACK。
- **A2-10**（A2 上机 bring-up，P1）：二审第二轮已通过大部分项（逐位一致 152/152、bd1=bd2 76/76 复算成立），只剩三处测试断言待补，PM 核完增量后会请示你合入授权。
